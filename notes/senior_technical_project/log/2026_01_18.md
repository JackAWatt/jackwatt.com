# 12:00 

I have started to figure out how to connect to wifi on the ESP32 and how to make an http get request.

### connection

```
#include <WiFi.h>
#include <HTTPClient.h>


void wifiSetUp(){
  WiFi.mode(WIFI_STA);
  WiFi.begin(ssid, password);
  Serial.print("Connecting to WiFi ..");
  while (WiFi.status() != WL_CONNECTED) {
    Serial.print('.');
    delay(1000);
  }
  Serial.println(WiFi.localIP());
}
```
